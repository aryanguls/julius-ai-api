{"python": "# Load the data and necessary libraries\nimport pandas as pd\nimport seaborn as sns\nimport matplotlib.pyplot as plt\nimport numpy as np\n\n# Read the CSV file\ndf = pd.read_csv('NBA Stats 202425 All Metrics NBA Player Props Tool.csv')\n\n# Calculate correlations\nnumeric_cols = df.select_dtypes(include=[np.number]).columns\ncorrelation_matrix = df[numeric_cols].corr()\n\n# Get the highest correlation pair (excluding self-correlations)\nmask = np.triu(np.ones_like(correlation_matrix), k=1)\ncorrelation_matrix_masked = correlation_matrix * mask\nmax_corr = np.max(correlation_matrix_masked)\nmax_corr_indices = np.where(correlation_matrix_masked == max_corr)\ncol1, col2 = numeric_cols[max_corr_indices[0][0]], numeric_cols[max_corr_indices[1][0]]\n\nprint(f\"The highest correlation ({max_corr:.3f}) is between {col1} and {col2}\")"}{"python": "# Create bar plot for top 10 scorers\nplt.figure(figsize=(12, 6))\ntop_10_scorers = df.nlargest(10, 'PPG')\nsns.barplot(data=top_10_scorers, x='NAME', y='PPG')\nplt.xticks(rotation=45, ha='right')\nplt.title('Top 10 NBA Scorers by PPG (2024-25)')\nplt.tight_layout()\nplt.show()"}